--[[
Lua stub preserved for compatibility with tooling that historically
expected a Lua formatter module.  The actual formatting implementation now
lives in :mod:`src.pretty.prettify` (Python) and this file remains as a
non-executable placeholder to satisfy legacy import paths without running
any payload code.
--]]
return {
    info = "See src.pretty.prettify (Python) for the active formatter implementation."
}
